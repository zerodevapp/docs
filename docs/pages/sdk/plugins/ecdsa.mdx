# ECDSA

The ECDSA validator validates signatures produced with a ECDSA private key, just like normal EOAs.  The ECDSA validator is used throughout our docs and you can [read more about it here](/sdk/core-api/create-account#standard-api).