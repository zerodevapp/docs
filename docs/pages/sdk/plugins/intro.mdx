# Plugins Introduction

Kernel is a *modular smart account*, which means it's a smart account that can be extended with *plugins*.

In Kernel parlance, there are two types of plugins:

- Validators: plugins that change how the account validates UserOps.
- Executors: plugins that 

plugins are known as "validators," because they determine how UserOps are validated.

Specifically, there are two types of validators:

- **Default validators** determine how UserOps and signatures are validated by default.
- **Non-default validators** determine how UserOps are validated conditionally, based on 