---
title: TypeScript / JavaScript SDK
description: Install and use the ZeroDev TypeScript SDK in a browser app.
---

# TypeScript / JavaScript SDK

{/* TODO(authoring): fill this stub. Sections below are the agreed template (Install / Setup / Example). Replace TODOs with real content. */}

:::info
🚧 **This page is a stub.** Content is being written. The structure below is the agreed template.
:::

The ZeroDev TypeScript SDK (`@zerodev/sdk`) is the core package for building smart-account apps in JavaScript/TypeScript. This page covers browser-side usage; for server-side use see [Node.js / TypeScript](/get-started/sdks/server-side/nodejs).

## Install

{/* TODO(authoring): confirm package list — is @zerodev/sdk enough, or do we recommend installing a validator (e.g. @zerodev/ecdsa-validator) here too? */}

```bash
npm i @zerodev/sdk
```

## Setup

{/* TODO(authoring): minimal browser-side initialization. Show kernelClient creation with viem chain + RPC. */}

```ts
// TODO: minimal initialization snippet
```

## Example

{/* TODO(authoring): "Hello World" — create a smart account, send a transaction. Link to a runnable repo. */}

```ts
// TODO: create-account + send-transaction example
```

## Next steps

- [Create a Smart Account](/smart-accounts/create-a-smart-account)
- [Authentication](/smart-accounts/authentication/social-login)
- [Quickstart](/get-started/quickstart)
