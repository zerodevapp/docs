---
title: C SDK (FFI)
description: Use the ZeroDev Omni SDK C FFI to embed smart-account functionality in any language.
---

# C SDK (FFI)

{/* TODO(authoring): fill this stub. The C binding is the FFI surface of the Omni SDK Zig core — used to embed in any language that can call C. */}

:::info
🚧 **This page is a stub.** Content is being written. The C FFI is in alpha — built from source.
:::

The ZeroDev C SDK exposes the Omni SDK's Zig core as a C-compatible shared library. Use it to embed ZeroDev smart-account functionality from languages that can call C — including those without a first-class Omni binding.

## Install

{/* TODO(authoring): confirm build instructions — clone, zig build, copy header + .so/.dylib. Or precompiled artifact URLs if any. */}

Build from source:

```bash
git clone https://github.com/zerodevapp/zerodev-omni-sdk
cd zerodev-omni-sdk
# TODO: build command and artifact paths
```

The header lives at `include/aa.h`.

## Setup

{/* TODO(authoring): minimal C example — header include, struct initialization. */}

```c
// TODO: #include "aa.h" + initialize
```

## Example

{/* TODO(authoring): "Hello World" — create a smart account, send a transaction. */}

```c
// TODO: end-to-end example
```

## Next steps

- [Omni SDK source](https://github.com/zerodevapp/zerodev-omni-sdk)
- [Bundler & Paymaster RPCs](/resources/infrastructure/rpcs)
